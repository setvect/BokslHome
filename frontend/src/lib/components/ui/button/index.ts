import Root from './button.svelte';
export { buttonVariants } from './types';
export type { ButtonProps, ButtonVariant, ButtonSize } from './types';

export { Root, Root as Button, Root as default };
